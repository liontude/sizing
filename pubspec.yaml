name: sizing
description: Flutter package for scaling UI elements proportionally across screen sizes. One extension for fonts, containers, padding, and radius — design proportions always preserved.
version: 2.0.1
homepage: https://github.com/liontude/sizing
repository: https://github.com/liontude/sizing
issue_tracker: https://github.com/liontude/sizing/issues

topics:
  - ui
  - responsive
  - layout
  - scaling

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0
