name: example
description: A new Flutter application.

version: 2.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sizing:

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  sizing:
    path: ../
